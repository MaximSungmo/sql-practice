-- select 기본
select * from departments;

select first_name, gender, hire_date from employees;

select concat(first_name,' ',last_name) as '이름' , gender as '성별', hire_date as '입사일' from employees;

select distinct title from titles ;

select concat(first_name, ' ',last_name)as name from employees where emp_no=10944;

select * from employees where first_name='Georgi Facello';

select emp_no, salary from salaries;

select concat(first_name, ' ',last_name)as name, gender as '성별', hire_date from employees where hire_date <= '1991-01-01' and gender='F';

SELECT emp_no, dept_no from dept_emp where dept_no in('d005', 'd009');

